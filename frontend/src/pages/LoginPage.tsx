import { PlaceholderPage } from './PlaceholderPage';

export function LoginPage() {
  return (
    <PlaceholderPage
      title="Sign in"
      description="Google OAuth sign-in lands here in a later phase."
    />
  );
}
